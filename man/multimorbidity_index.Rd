% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multimorbidity_index.R
\name{multimorbidity_index}
\alias{multimorbidity_index}
\title{Calculate a multimorbidity index from self-reported conditions}
\usage{
multimorbidity_index(
  data,
  conditions,
  weights = NULL,
  na_action = c("ignore", "na")
)
}
\arguments{
\item{data}{A data frame with one row per respondent.}

\item{conditions}{A character vector of column names in \code{data}. Each
column must already be coded 0/1/NA (1 = condition present). Use
\code{recode_binary()} first if your raw data uses Yes/No, 1/2, etc.}

\item{weights}{Optional named numeric vector giving a weight for each
condition (names must match \code{conditions}). If \code{NULL} (the default),
every condition counts equally -- i.e. a simple unweighted sum, which
is how the published Functional Comorbidity Index (Groll et al. 2005)
is scored. See \code{fci_items()}.}

\item{na_action}{How to handle rows with at least one missing condition.
\code{"ignore"} (default) computes scores from available non-missing
conditions. \code{"na"} sets the respondent's index fields to \code{NA}.}
}
\value{
\code{data} with three appended columns: \code{mm_n_conditions} (count of
conditions present), \code{mm_index} (possibly weighted score), and
\code{mm_category} (\code{"None"}, \code{"Single condition"}, \code{"Multimorbid"}).
}
\description{
Computes respondent-level multimorbidity from binary condition indicators,
tailored to survey datasets where conditions are self-reported rather than
ICD-coded.
}
\examples{
df <- data.frame(
  id           = 1:5,
  hypertension = c(1, 0, 1, 1, 0),
  diabetes     = c(0, 0, 1, 1, 0),
  arthritis    = c(1, 0, 0, 1, NA)
)
multimorbidity_index(df, conditions = c("hypertension", "diabetes", "arthritis"))
}
\references{
Groll, D. L., To, T., Bombardier, C., & Wright, J. G. (2005). The
development of a comorbidity index with physical function as the outcome.
\emph{Journal of Clinical Epidemiology}, 58(6), 595-602.
\doi{10.1016/j.jclinepi.2004.10.018}
}
